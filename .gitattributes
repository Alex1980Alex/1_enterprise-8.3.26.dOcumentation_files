# Auto detect text files and perform LF normalization

# Автоматическое определение и нормализация окончания строк
* text=auto

# Всегда использовать LF (Unix) для файлов кода и документации
*.bsl      text eol=lf
*.txt      text eol=lf
*.md       text eol=lf
*.json     text eol=lf
*.xml      text eol=lf
*.yml      text eol=lf
*.yaml     text eol=lf

# Windows-специфичные файлы (если нужно)
*.bat      text eol=crlf
*.cmd      text eol=crlf

# Бинарные файлы — не обрабатывать как текст
*.png      binary
*.jpg      binary
*.jpeg     binary
*.gif      binary
*.ico      binary
*.pdf      binary
*.zip      binary
*.rar      binary

# Файлы 1С конфигурации (если выгружаются в text-формате)
*.xml      text eol=lf
*.xsd      text eol=lf
